<div id="<%= dom_id category %>">
  <p>
    <strong>Name:</strong>
    <%= category.name %>
  </p>

  <% if category.image.attached? %>
    <div>
      <%= image_tag (category&.image.attached? ? category.image : ""), style: 'height: 40px; weight: 40px;' %>
    </div>
  <% end %>

</div>
